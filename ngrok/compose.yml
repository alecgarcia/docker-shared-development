services:
  ngrok:
    image: ngrok/ngrok
    restart: unless-stopped
    volumes:
      - ../ngrok.yml:/ngrok.yml
    ports:
      - 127.0.0.1:4040:4040
    command:
      - "start"
      - "--all"
      - "--config"
      - "ngrok.yml"
    networks:
      - sail